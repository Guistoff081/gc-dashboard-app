<%= form_with(model: venda, local: true) do |form| %>
  <% if venda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venda.errors.count, "error") %> prohibited this venda from being saved:</h2>

      <ul>
      <% venda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cvalor_total %>
    <%= form.text_field :cvalor_total, id: :venda_cvalor_total %>
  </div>

  <div class="field">
    <%= form.label : %>
    <%= form.text_field :, id: :venda_ %>
  </div>

  <div class="field">
    <%= form.label :keys %>
    <%= form.text_field :keys, id: :venda_keys %>
  </div>

  <div class="field">
    <%= form.label :clientes_id %>
    <%= form.text_field :clientes_id, id: :venda_clientes_id %>
  </div>

  <div class="field">
    <%= form.label :produtos_id %>
    <%= form.text_field :produtos_id, id: :venda_produtos_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
