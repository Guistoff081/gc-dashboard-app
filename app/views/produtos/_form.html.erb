<%= bootstrap_form_with(model: produto, local: true) do |form| %>
  <% if produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(produto.errors.count, "error") %> prohibited this produto from being saved:</h2>

      <ul>
      <% produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :nome, id: :produto_nome %>
  </div>

  <div class="field">
    <%= form.text_field :marca, id: :produto_marca %>
  </div>

  <div class="field">
    <%= form.number_field :quantidade, id: :produto_quantidade %>
  </div>

  <div class="field">
    <%= form.text_field :price, id: :produto_price %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary', value: "Confirmar"%>
  </div>
<% end %>
